<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- 1. PATIENT VIEW (Inherited from Contacts) -->
    <record id="view_partner_form_hospital" model="ir.ui.view">
        <field name="name">res.partner.form.hospital</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='function']" position="after">
                <field name="is_patient"/>
                <field name="blood_group" invisible="not is_patient"/>
                <field name="age" invisible="not is_patient"/>
                <field name="gender" invisible="not is_patient"/>
            </xpath>
        </field>
    </record>

    <!-- 2. APPOINTMENT VIEW (With Chatter) -->
    <record id="view_appointment_form" model="ir.ui.view">
        <field name="name">hospital.appointment.form</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="patient_id" context="{'default_is_patient': True}"/>
                            <field name="doctor_id"/>
                        </group>
                        <group>
                            <field name="date"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_appointment_list" model="ir.ui.view">
        <field name="name">hospital.appointment.list</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'done'" decoration-info="state == 'confirmed'">
                <field name="name"/>
                <field name="patient_id"/>
                <field name="doctor_id"/>
                <field name="date"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <!-- 3. STAFF & DEPARTMENT VIEWS (Configuration) -->
    <record id="view_staff_list" model="ir.ui.view">
        <field name="name">hospital.staff.list</field>
        <field name="model">hospital.staff</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="role"/>
                <field name="department_id"/>
            </list>
        </field>
    </record>

    <!-- 4. ACTIONS -->
    <record id="action_hospital_patient" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('is_patient', '=', True)]</field>
        <field name="context">{'default_is_patient': True}</field>
    </record>

    <record id="action_hospital_appointment" model="ir.actions.act_window">
        <field name="name">Appointments</field>
        <field name="res_model">hospital.appointment</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_hospital_staff" model="ir.actions.act_window">
        <field name="name">Staff</field>
        <field name="res_model">hospital.staff</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_hospital_department" model="ir.actions.act_window">
        <field name="name">Departments</field>
        <field name="res_model">hospital.department</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- 5. MENUS (Organized Structure) -->
    <menuitem id="menu_hospital_root" name="Hospital" sequence="10"/>

    <!-- Operations Category -->
    <menuitem id="menu_hospital_operations" name="Operations" parent="menu_hospital_root" sequence="10"/>
    <menuitem id="menu_patient" name="Patients" parent="menu_hospital_operations" action="action_hospital_patient" sequence="10"/>
    <menuitem id="menu_appointment" name="Appointments" parent="menu_hospital_operations" action="action_hospital_appointment" sequence="20"/>

    <!-- Configuration Category -->
    <menuitem id="menu_hospital_config" name="Configuration" parent="menu_hospital_root" sequence="100"/>
    <menuitem id="menu_staff" name="Staff" parent="menu_hospital_config" action="action_hospital_staff" sequence="10"/>
    <menuitem id="menu_department" name="Departments" parent="menu_hospital_config" action="action_hospital_department" sequence="20"/>

</odoo>